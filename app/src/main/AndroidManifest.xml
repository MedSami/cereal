<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aziza.cereal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Arrivage"></activity>
        <activity android:name=".ListTransactions" />
        <activity android:name=".ModifierMotDePasse" />
        <activity android:name=".PasserCommande" />
        <activity android:name=".MenuTransformateur" />
        <activity android:name=".MenuCollecteur" />
        <activity android:name=".MenuAgriculteur" />
        <activity android:name=".Inscrire" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>